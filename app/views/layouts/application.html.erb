<!DOCTYPE html>
<html>
<head>
  <title>Blocipedia</title>
   <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="pull-right user-info">
    <% if user_signed_in? %>
     Signed in as: <strong><%= current_user.email %></strong> |
     <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
     <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link'  %> -
     <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
   <% end %>
   <% if user_signed_in? %>
     <% if current_user.standard? %>
        <p><%= link_to "Upgrade your account to premium to Blocipedia", new_charge_path(current_user) %> </p>
      <% else %>
        <p><%= link_to "Cancel your premium a/c", charge_path(current_user), method: :delete,  data: {confirm: 'Are you sure you want to unsubscribe?'}  %> </p>
        <% end %>
    <% end %>
  </div>
  <!-- <div class="row">
    <div class="col-md-8">
      <p><%= link_to "Subscribe", new_charge_path %> </p>
    </div>
  </div> -->
  <div class="row">
    <div class="col-md-8">
      <p class="wiki-list"><%= link_to "Wikis", wikis_path %> </p>
    </div>
  </div>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
<%= yield %>

</body>
</html>
