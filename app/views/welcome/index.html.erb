<h1>Welcome to Blocipedia!</h1>
  <div class="row">
    <div class="col-md-8">
      <% if current_user.standard? %>
        <p><%= link_to "Upgrade your account to premium to Blocipedia", new_charge_path(current_user) %> </p>
      <% else %>
        <p><%= link_to "Cancel your premium a/c", charge_path(current_user), method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to unsubscribe?'}  %> </p>
      <% end %>
    </div>
  </div>
